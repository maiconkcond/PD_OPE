<p id="notice"><%= notice %></p>

<h1>Listing Fabricantes</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Tel1</th>
      <th>Tel2</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fabricantes.each do |fabricante| %>
      <tr>
        <td><%= fabricante.nome %></td>
        <td><%= fabricante.tel1 %></td>
        <td><%= fabricante.tel2 %></td>
        <td><%= link_to 'Lista', fabricante %></td>
        <td><%= link_to 'Editar', edit_fabricante_path(fabricante) %></td>
        <td><%= link_to 'Excluir', fabricante, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fabricante', new_fabricante_path %>
